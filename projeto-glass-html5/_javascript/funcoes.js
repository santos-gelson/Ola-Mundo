/* Criar uma área de script para ativar a função que muda a foto quando o mouse passa sobre o menu.(método muda foto) */

function mudaFoto (foto) {
    document.getElementById("icone").src = foto;
}

